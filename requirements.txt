# Core Discord library
discord.py==2.3.2

# Media downloader — intentionally NOT pinned!
# yt-dlp must always be up-to-date for YouTube compatibility.
# Pinning yt-dlp causes "Requested format is not available" errors
# because YouTube regularly changes its available formats.
yt-dlp>=2025.1.1

# Environment variable loading
python-dotenv==1.0.0

# Async HTTP client
aiohttp==3.9.0

# Instagram media extraction
instaloader==4.12.0
