# Core Discord library
discord.py==2.3.2

# Media downloader — intentionally NOT pinned!
# yt-dlp must always be up-to-date for YouTube/Instagram compatibility.
yt-dlp>=2025.1.1

# Environment variable loading
python-dotenv==1.0.0

# Async HTTP client (fallback image downloader)
aiohttp==3.9.0

# Instagram session-based media extraction
instaloader>=4.10
